extends layout

block content
  div.d-flex.flex-column
    h1= title

    
    div.d-flex.flex-wrap
      each movie in movie_list
        div.img-container
          a(href=movie.url)
            img.list(src=movie.cover)
          
      else
        li There are no movies.

  