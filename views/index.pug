extends layout

block content
  h1= title
  p  #{title}
  
  ul
    each movie in movie_list
      li
        a(href=movie.url) #{movie.title} 
        |  (#{movie.year_made})
  
    else
      li There are no books.

  