extends layout

block content
  h1= title
  p  #{title}
  
  ul
    each movie in movie_list
      a(href=movie.url)
        img.list(src=movie.cover)
        
    else
      li There are no movies.

  